# QuantGold Complete Procfile for Railway
# Production deployment with all features

web: gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:$PORT quantgold_complete_app:app --timeout 120 --max-requests 1000
